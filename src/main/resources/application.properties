spring.datasource.url = jdbc:postgresql://localhost:5432/tps
spring.datasource.username = postgres
spring.datasource.password =

spring.jpa.hibernate.ddl-auto = none

spring.liquibase.enabled = true
spring.liquibase.change-log = classpath:/liquibase/mysql/db-changelog-master.xml
spring.liquibase.drop-first = false
spring.liquibase.liquibase-schema = tps
spring.liquibase.default-schema = tps
spring.liquibase.user = postgres
spring.liquibase.password =

spring.level.org.hibernate.SQL = DEBUG